name: prometheus-node-exporter

on:
  push:
    branches:
      - main
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1

      - name: Set up Docker Buildx
        id: buildx
        uses: docker/setup-buildx-action@v1

      # - name: Configure AWS credentials
      #   uses: aws-actions/configure-aws-credentials@v1
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID_BRUCE }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY_BRUCE }}
      #     aws-region: ap-northeast-2

      # - name: Login to Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v1

      # - name: Docker pull and push
      #   run: |
      #     docker pull quay.io/prometheus/node-exporter:v1.3.1
      #     docker image inspect quay.io/prometheus/node-exporter:v1.3.1

      #     docker tag quay.io/prometheus/node-exporter:v1.3.1 ${{ steps.login-ecr.outputs.registry }}/mirror/prometheus/node-exporter:v1.3.1
      #     docker images
      #     docker push ${{ steps.login-ecr.outputs.registry }}/mirror/prometheus/node-exporter:v1.3.1

      - name: Build & Push to AWS ECR Private
        uses: opspresso/action-docker@master
        with:
          args: --ecr
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID_BRUCE }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY_BRUCE }}
          AWS_REGION: "ap-northeast-2"
          BASE_IMAGE: quay.io/prometheus/node-exporter
          IMAGE_NAME: quay.io/prometheus/node-exporter
          TAG_NAME: "v1.3.1"
          PLATFORM: "linux/amd64,linux/arm64"
